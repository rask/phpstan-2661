{
    "name": "rask/phpstan-2661",
    "description": "Bug reproduction project",
    "license": "MIT",

    "require": {
        "php": ">=7.2"
    },

    "require-dev": {
        "phpstan/phpstan": "^0.12.0",
        "phpstan/phpstan-phpunit": "^0.12.0"
    },

    "autoload-dev": {
        "psr-4": {
            "App\\Tests\\": "tests/"
        }
    },

    "scripts": {
        "phpstan": "tools/phpstan analyze",
        "test": "tools/phpunit.phar tests"
    }
}
